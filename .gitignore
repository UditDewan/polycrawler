# data is gitignored (DVC-tracked from Phase 7)
/data/
*.duckdb
*.duckdb.wal

# python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# secrets — never commit these (this repo is public)
.env
.env.*
!.env.example
*.key
*.pem
secrets.*
*credentials*
*.secret

# graphify generated artifacts
/graphify-out/

# mlflow local stores (DVC/registry artifacts live under data/, also ignored)
/mlruns/
/mlartifacts/
*.db
